using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Project : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("프로젝트를 시작합니다");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
